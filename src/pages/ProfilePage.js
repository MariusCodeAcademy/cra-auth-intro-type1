function ProfilePage() {
  // to get profile info we send toke with fetch

  // get all users posts from
  // get http://localhost:5000/posts
  // send token

  return (
    <main>
      <h1>profile</h1>
    </main>
  );
}

export default ProfilePage;
